import React from "react";

export default class Soundwave extends React.Component{

    render(){
        return(
            <section className={this.props.cssClass}>
            </section>
        )
    }
}